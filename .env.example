# Values interpolated into docker-compose.yml. Copy to `.env`.
# The database URL is set inside compose (points at the `mongo` service).
JWT_SECRET="replace-with-a-long-random-secret"
JWT_EXPIRES_IN="7d"
